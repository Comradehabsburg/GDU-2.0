on_actions = {
		

	#ROOT is new controller #FROM is old controller #FROM.FROM is state ID

	##Island Hopping
	on_state_control_changed = {
		effect = {
			if = {
				limit = {
					FROM = { tag = RAJ }
					ROOT = { 
						OR = { 
							is_in_faction_with = JAP
							tag = JAP
						} 
					}
					FROM.FROM = {
						has_state_flag = RAJ_controls
					}
				}
				FROM.FROM = {  
				 	set_state_flag = JAP_burma_controls
				 	clr_state_flag = RAJ_controls
				}
			 	JAP = { 
			 		JAP_burma_island_hopping_level_up = yes
			 	}
		 		RAJ = {
		 			RAJ_island_hopping_level_down = yes 
		 		}
			}
		}
	}
	on_state_control_changed = {
		effect = {
			if = {
				limit = {
					FROM = { 
					    OR = { 
							is_in_faction_with = JAP 
							tag = JAP
						}
					}
					ROOT = {
						OR = {
						 tag = RAJ
						 is_in_faction_with = RAJ
						} 
					}
					FROM.FROM = {
						has_state_flag = JAP_burma_controls
					}
				}
				FROM.FROM = { 
				 	set_state_flag = RAJ_controls 
				 	clr_state_flag = JAP_burma_controls 
				}
				RAJ = {
				 RAJ_island_hopping_level_up = yes
				}  
		 		JAP = {
					JAP_burma_island_hopping_level_down = yes
		 		} 
			}
		}
	}
	
	




# AUSTRALIA SECTION
# AUSTRALIA SECTION
# AUSTRALIA SECTION
# AUSTRALIA SECTION
# AUSTRALIA SECTION


	on_state_control_changed = {
		effect = {
			if = {
				limit = {
					FROM = { tag = AST }
					ROOT = { 
						OR = { 
							is_in_faction_with = JAP
							tag = JAP
						} 
					}
					FROM.FROM = {
						has_state_flag = AST_controls
					}
				}
				FROM.FROM = {  
				 	set_state_flag = JAP_australia_controls
				 	clr_state_flag = AST_controls
				}
			 	JAP = { 
			 		JAP_australia_island_hopping_level_up = yes
			 	}
		 		AST = {
		 			AST_island_hopping_level_down = yes 
		 		}
			}
		}
	}
	on_state_control_changed = {
		effect = {
			if = {
				limit = {
					FROM = { 
					    OR = { 
							is_in_faction_with = JAP 
							tag = JAP
						}
					}
					ROOT = {
						OR = {
						 tag = AST
						 is_in_faction_with = AST
						} 
					}
					FROM.FROM = {
						has_state_flag = JAP_australia_controls
					}
				}
				FROM.FROM = { 
				 	set_state_flag = AST_controls 
				 	clr_state_flag = JAP_australia_controls 
				}
				AST = {
				 AST_island_hopping_level_up = yes
				}  
		 		JAP = {
					JAP_australia_island_hopping_level_down = yes
		 		} 
			}
		}
	}
	
	#
	#
	#
	#
}